// The native app talks to the existing API directly (RN fetch isn't subject to
// browser CORS). Always the real shop (no playground from the app).
export const API_BASE = "https://lgyapi-production.up.railway.app/api";
