NEXT_PUBLIC_API_URL="http://localhost:4000"
# Server-only: where Next.js proxies /api requests to (and where server components
# call the API directly). In production set this to your Railway URL.
API_URL="http://localhost:4000"
